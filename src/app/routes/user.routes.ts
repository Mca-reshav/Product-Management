export const BASE_URL = 'http://localhost:6001/web/user';

export const API_ROUTES = {
  register: `${BASE_URL}/register`,
  login: `${BASE_URL}/login`,
  profile: `${BASE_URL}/profile`,
  toggleRole: `${BASE_URL}/toggleRole`,
  listUsers: `${BASE_URL}/list`,
};
